# 📋 [김진우 님 전용] 비자명 충돌 (Rename vs Modify) 실전 실습 가이드

> **실습 목표**: 코디세이 과제 필수 만점 항목인 **「비자명 충돌(Non-trivial Conflict) 1회 이상 해결 증빙」**을 완결하기 위해, 김진우 님과 안재현 님이 합을 맞추어 `Rename vs Modify` 충돌을 유발하고 해결합니다.

---

## 💡 1. 실습 배경 및 개요 (왜 하는가?)

- **자명한 충돌 (충돌 1 - 완료)**: 같은 줄을 동시에 고쳐서 생기는 단순 충돌 (`food_data.json`)
- **비자명 충돌 (충돌 2 - 지금 할 일)**:  
  **김진우 님이 파일 이름을 변경(`git mv`)**하고, **안재현 님이 옛날 파일 내용을 수정**했을 때 발생하는 구조적 충돌입니다.  
  Git이 파일 추적 과정에서 `CONFLICT (rename/modify)`를 발생시키며, 이를 해결하는 과정을 Git 히스토리와 `docs/conflict-resolution.md`에 증빙으로 남깁니다.

---

## ⚡ 2. [김진우 님] 터미널에서 딱 4줄만 실행해 주세요! (1분 컷)

김진우 님의 VS Code 터미널에서 아래 명령어를 **순서대로 복사해서 실행**해 주시면 됩니다:

```powershell
# 1. main 최신화 및 진우 님 전용 실습 브랜치 생성
git checkout main
git pull origin main
git checkout -b feature/kim-rename-tiebreaker

# 2. lottery.py 파일 이름을 tie_breaker.py 로 변경 (git mv 필수!)
git mv src/lottery.py src/tie_breaker.py

# 3. 변경 사항 커밋 (컨벤션 준수)
git commit -m "refactor: Rename lottery.py to tie_breaker.py for module clarity"

# 4. 깃허브 원격 저장소로 푸시
git push -u origin feature/kim-rename-tiebreaker
```

> ⚠️ **주의**: 파일명을 바꿀 때 탐색기나 에디터에서 바꾸지 마시고, 반드시 **`git mv src/lottery.py src/tie_breaker.py`** 명령어를 사용해야 Git이 Rename으로 정상 추적합니다!

---

## 🔄 3. 그 이후 진행 과정 (안재현이 처리할 일)

김진우 님이 2번의 푸시를 완료해 주시면, 안재현 님이 아래 단계를 이어 진행합니다:

1. **안재현 브랜치(`feature/ahn-modify-lottery`) 작업**:  
   - 안재현이 `src/lottery.py` 파일 내부의 추첨 독스트링/알고리즘을 수정하고 커밋.
2. **머지 시도 ➔ 비자명 충돌 유발**:  
   - 진우 님의 `feature/kim-rename-tiebreaker` 브랜치를 머지할 때 터미널에 아래와 같이 **비자명 충돌 경고가 쾅! 발생**합니다:
     ```text
     CONFLICT (rename/modify): src/lottery.py renamed to src/tie_breaker.py in HEAD.
     Version feature/ahn-modify-lottery of src/lottery.py left in tree.
     Automatic merge failed; fix conflicts and then commit the result.
     ```
3. **충돌 해결 (3-Way Merge)**:  
   - 안재현이 개선한 로직을 새 파일 `src/tie_breaker.py` 내부로 안전하게 합치고, 잔여 옛날 파일 `src/lottery.py`를 `git rm`으로 정리 후 머지 커밋 생성.
4. **증빙 문서 반영**:  
   - 실제 생성된 머지 커밋 해시를 `docs/conflict-resolution.md` 충돌 기록 #2에 반영하여 과제 채점 증빙 완성!

---

## 🏆 완료 후 기대 효과

- 김진우 님의 Git 브랜치 작업 및 원격 푸시 기여도 실적 확보
- 평가관(멘토)이 검사하는 `git log --graph` 상에 **진짜 비자명 충돌 및 해결 머지 커밋**이 완벽하게 증명됨!